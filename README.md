# get-stock
Python script to extract image metadata from Shutterstock and Adobe Stock HTML exports. Outputs CSV-style lines with platform, filename, ID, and title. Auto-detects platform and decodes HTML/URL-encoded titles. Works with wildcards for batch processing.
